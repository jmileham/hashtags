<%= simple_form_for @tag, url: tag_path do |f| %>
    <%= f.input :parent_content, as: :hidden %>
    <%= f.input :content, label: '#', autofocus: true %>
    <%= f.submit "hash", class: 'btn btn-default' %>
<% end %>
